{
  "buildCommand": "yarn install --cwd frontend && yarn --cwd frontend build",
  "outputDirectory": "frontend/dist",
  "framework": null,
  "installCommand": "echo 'Skipping root install'",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
